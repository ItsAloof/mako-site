import { NextPage } from "next";
import { GuildInfo } from '../utils/DataTypes'

interface GuildProp {
    guild: GuildInfo
}

const GuildPage = ({ guild }: { guild: GuildInfo }) => 
{
    return (<></>)
}

export default GuildPage